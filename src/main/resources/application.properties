# Railway provides the full JDBC URL including database name
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

# Use Railway's dynamic port (defaults to 8080 locally)
server.port=${PORT:8080}

# Hibernate configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Mail configuration (keep as is, but consider using env vars for security)
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=nileshpatil2794@gmail.com
#spring.mail.password=half jtza hwly fbuo

#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true